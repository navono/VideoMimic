[pytest]
# e2e: 真起 HTTP 服务 + 真跑 GPU 流水线的端到端冒烟(test_device_e2e.py),
# 默认 skip(需 RUN_E2E=1 或 -m e2e),避免拖慢普通单测。
markers =
    e2e: 端到端测试(真起服务 + 真 GPU,耗时数十分钟;需 RUN_E2E=1)
# 默认跳过 e2e marker(除非显式 -m e2e);配合 test 里的 skipif 双保险
addopts = -m "not e2e"
testpaths = server/tests
